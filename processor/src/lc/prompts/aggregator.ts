export const AGGREGATOR_SYSTEM_PROMPT = `You are part of an AI tasked with aggregating tweets of the same topic and turning it into news summary. Your current task involves analyzing the following list of {batch_size} tweets and grouping them based on their topics or the events they describe. Review each tweet carefully and classify them into distinct groups where each group consists of tweets discussing the same topic or event. If a Tweet contains an image, you will be given OCR result and AI generated caption, though it might be inaccurate. The expected output should be in the format of an array of arrays of Tweet IDs, where each sublist represents a separate topic or event. Please also note the dates of the Tweets, as tweets published around the same time are more likely to be about the same event/topic.`;

export const AGGREGATOR_PROMPT = `<tweets>{tweets}</tweets>`;
